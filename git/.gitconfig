[user]
	name = Kaito Mateu
	email = kaito@cryptolab.net 
[alias]
    pl = log --graph --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold cyan)%aD%C(reset) %C(bold green)(%ar)%C(reset)%C(bold yellow)%d%C(reset)%n''          %C(white)%s%C(reset) %C(dim white)- %an%C(reset)' --all
    st = status
    co = checkout
    br = branch
    rb = rebase
    ci = commit
