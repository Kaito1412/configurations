import Ember from 'ember';

export default Ember.Controller.extend({
    actions: {
       createPost: function () {
           var title = this.get('newPostTitle');
           var body = this.get('newPostBody');

           var post = this.store.createRecord('post', {title: title, body: body});
           post.save()
        }
    }
});
