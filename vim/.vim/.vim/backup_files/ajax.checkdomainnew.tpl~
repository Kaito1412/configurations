{* MULTICURRENCY *}
{php}
require_once "includes/modules/Other/gigasmcp/Multicurrency.php";
$mu = new Multicurrency();
foreach($mu->convertDomainPrices($this->get_template_vars()) as $k => $v) $this->assign($k, $v);
{/php}
{* MULTICURRENCY *}

{if $whois}
  {$whois}
{/if}
{if $check}
  {if $bulk && !$transfer}
    <input type='hidden' name='domain' value='illregister'/>
    {foreach from=$check item=ccme name=foo}
      {if $ccme.status == "ok"}
	<tr>
	  <td class="le dom">
            <img alt="ok" src="{$template_dir}images/dom-ok.png" />
            {$ccme.sld}{$ccme.tld}
          </td>
          <td>{$lang.availorder}</td>
          <td align="center">
            <input type="checkbox" name="tld[{$ccme.sld}{$ccme.tld}]" value="{$ccme.tld}" checked="checked"/>
	    <input type="hidden" name="sld[{$ccme.sld}{$ccme.tld}]" value="{$ccme.sld}" />
          </td>
	  <td>
	    <select style="padding: 0;" name="period[{$ccme.sld}{$ccme.tld}]">
                {$ccme.prices|var_dump}
              {php}
                  foreach ($ccme.prices as $key => $row) {
                      $period[$key] = $row['period'];
                  }
                  array_multisort($period, SORT_DESC, $ccme.prices);
              {/php}
	      {foreach from=$ccme.prices item=price}
		{if $transfer && $price.transfer>=0}
		  <option value="{$price.period}">{$price.period} {$lang.years} @ {$price.transfer|price:$currency}</option>
                {elseif !$transfer && $price.register>=0}
		  <option value="{$price.period}">{$price.period} {$lang.years} @ {$price.register|price:$currency}</option>
		{/if}                    
	      {/foreach}
	    </select>
	  </td>
	</tr>
      {else}
	<tr>
          <td class="le dom">
            <img alt="unavailable" src="{$template_dir}images/dom-no.png">
            {$ccme.sld}{$ccme.tld}
          </td>
	  <td>
	    {if $transfer}
	      {$lang.trans_unavail}  <br />
	      <a href="{$ca_url}cart&amp;step=1&amp;domain=illregister&amp;sld={$ccme.sld}&amp;tld={$ccme.tld}&amp;period=1&amp;ref=1&amp;cat_id=register">{$lang.trans_reg}</a>
	    {else}
	      {$lang.unavail}
	    {/if}
          </td>
	  <td align="center">
            <input type="checkbox" disabled="disabled" />
          </td>
	  {* escondemos www y whois
          <td>
	    <div>
              <div>
                <a href="http://www.{$ccme.sld}{$ccme.tld}" target="_blank">WWW</a>&nbsp;
                <a href="{$ca_url}checkdomain&action=whois&amp;sld={$ccme.sld}&amp;tld={$ccme.tld}" onclick="window.open(this.href,'{$ccme.sld}{$ccme.tld}','width=500, height=500, scrollbars=1'); return false">WHOIS</a>
	      </div>
            </div>
	  </td>
          *}
          <td></td>
	</tr>

      {/if}
    {/foreach}
  {else}
    {if $transfer}
      <input type='hidden' name='domain' value='illtransfer'/>
    {else}
      <input type='hidden' name='domain' value='illregister'/>
    {/if}
    {if $check.status == 'ok'}

      <input type="hidden" name="tld[{$check.sld}{$check.tld}]" value="{$check.tld}"/>
      <input type="hidden" name="sld[{$check.sld}{$check.tld}]" value="{$check.sld}" />
      {$lang.domain} <span style="color: #000">{$check.sld}{$check.tld}</span> <span style="color: #00CC00; font-weight: bold; text-transform: uppercase">{$lang.Available}!</span> <div>{$lang.chooseperiod}: <select name="period[{$check.sld}{$check.tld}]">
        {php}
            foreach ($check.prices as $key => $row) {
                $period[$key] = $row['period'];
            }
            array_multisort($period, SORT_ASC, $check.prices);
        {/php}
	{foreach from=$check.prices item=price}
	  {if $transfer && $price.transfer>=0}
	    <option value="{$price.period}">{$price.period} {$lang.years} @ {$price.transfer|price:$currency}</option>
          {elseif !$transfer && $price.register>=0}
	    <option value="{$price.period}">{$price.period} {$lang.years} @ {$price.register|price:$currency}</option>
	  {/if}

	{/foreach}
      </select></div>
      <div class="button06" style="float:none; margin: 20px  0px 20px 150px; width: 130px;"><div><input type="submit" value="{$lang.ordernow}"></div></div>
    {else}
      <div style="margin: 20px 0">
        {$lang.domain}
        <span style="color: #000">{$check.sld}{$check.tld}</span> 
        <span style="color: #CC0000; font-weight: bold; text-transform: uppercase">{$lang.Unavailable}</span></div>
    {/if}
  {/if}
{/if}
